#include <stdio.h>

int main()
{
    char Array[6] = {'A', 'p', 't', 'e', 'c', 'h'};
    int i;
    for (int i = 0; i < 6; i++)
    {
        printf("The elemnt %d of the array is %c\n", i, Array[i]);
    }

       return 0;
}